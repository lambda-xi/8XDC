#include <stdio.h>
#include <stdlib.h>

/**
 * Opens an 8xp file and checks for integrity.
 *
 * @param string path is the path to the source .8xp
 * @return a file pointer to the newly validated .8xp
 */
FILE* open8xp(char* path);
